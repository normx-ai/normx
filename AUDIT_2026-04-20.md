# Audit Normx — 2026-04-20

Application React 19 + Express + PostgreSQL + Qdrant (comptabilité, paie, états, révision, assistant IA).
Audit réalisé sur trois axes : sécurité, performance, duplication de code.

Légende d'état : `[x]` = corrigé / `[ ]` = en attente / `[~]` = ajusté (diagnostic révisé à l'analyse).

Corrections committées : `20e0e5e` (sprint 1 & 2), `8d6e84f` (docs), + vague 3 en cours (taxation, formatDate, types backend, CORS, Qdrant timeout).

---

## 1. Sécurité

### Critique

- [~] JWT `/me` sans vérification de signature RS256 (pas seulement `exp`)
  - Diagnostic révisé : `/callback` et `/refresh` reçoivent le token directement de Keycloak via TLS, donc authentiques. Le vrai risque était sur `/me` qui acceptait un cookie forgé.
  - Fait : `server/routes/auth.ts:162` passe par `authenticateToken` (JWKS + RS256 + iss + exp).

- [x] XSS via `dangerouslySetInnerHTML` sur exports Excel
  - `src/etats/ResultatFiscal.tsx:597`, `CompteResultatSYSCOHADA.tsx:576`, `TFT_SYSCOHADA.tsx:620`, `BilanSYSCOHADA.tsx:441`
  - Fait : `src/lib/excelExport.ts` → helper `escapeHtml` appliqué sur `entiteName`, `entiteNif`, `title`, `subtitle`, `headers`, `row.libelle`, `row.ref`, `exerciceAnnee`, `dureeMois`, valeurs string.

- [~] Cookie CSRF `httpOnly: false`
  - Diagnostic révisé : ce réglage est exigé par le pattern double-submit, le frontend lit le cookie via `document.cookie` (src/csrf-fetch.ts:16, src/api.ts:10). Défense réelle = `sameSite: 'strict'` déjà en place. Pas de régression accepté.
  - Fait : `server/middleware/csrf.ts:22` annoté pour documenter le choix.

### Haute

- [x] DB SSL désactivé par défaut en prod
  - Diagnostic révisé : l'infra prod utilise PostgreSQL sur réseau Docker interne (`normx-db`), pas d'exposition externe. SSL opt-in est correct.
  - Fait : `server/db.ts` émet un `logger.warn` au démarrage si `NODE_ENV=production` et host externe sans `DB_SSL=true`.

- [ ] Message d'erreur verbeux sur `ANTHROPIC_API_KEY`
  - `server/services/assistant.chat.ts:11-12`, `server/services/ocr-import.service.ts:13-14`
  - Non traité : non critique. À faire : validation au startup + erreur générique côté client.

### Moyenne

- [ ] Vérifier qu'aucun stack trace ne fuit au client (comportement actuel OK, à garder sous surveillance).
  - `server/index.ts:213-215`

- [x] CORS fallback `localhost` si `ALLOWED_ORIGINS` non défini ; garde anti-`*` à ajouter.
  - Fait : `server/index.ts` throw au démarrage si `ALLOWED_ORIGINS` contient `*` (config invalide fail-fast).

### Propre (inchangé)

SQL paramétré, Helmet, rate limit auth 10/h, JWT RS256 via JWKS Keycloak, Multer magic bytes, pas d'`eval`, cookies `httpOnly: true` sur tokens d'accès/refresh.

---

## 2. Performance

### Critique

- [x] Pagination absente ou `LIMIT 5000` hardcodé
  - `server/services/ecritures.service.ts:360` (`getGrandLivreTiers`)
  - `server/services/ecritures.service.ts:484` (`getBalanceAgee`)
  - `server/services/ecritures.service.ts:607` (`getEcheancier`)
  - Fait : paramètre `{ limit, offset }` injecté via `getPagination(req, 1000, 5000)` dans les 3 routes. Plafond max toujours à 5000 pour compatibilité existante.

- [ ] Import TVA : deux `SELECT` massifs sur comptes 443x/445x puis calculs JS
  - `server/services/tva.service.ts:354-400`
  - Non traité. À faire : calculer le net en SQL (`montant_taxe / taux`) au lieu de la boucle JS.

### Important

- [x] Calculs Bilan sans `useMemo`
  - `src/etats/BilanSMT.tsx:154-159`
  - Fait : `useMemo` sur `computeActif`, `computePassif`, `computeResultatNet` avec déps `[lignesN]`/`[lignesN1]`.

- [ ] `readFileSync` runtime sur KB et templates SQL
  - `server/services/assistant.agents.ts`, `server/services/tenant.service.ts`
  - Non traité. À faire : chargement unique au startup ou `fs/promises`.

- [x] Qdrant + fallback KB séquentiels
  - Fait : `server/services/assistant.search.ts` enveloppe `qdrantModule.search` dans `Promise.race` avec timeout 2 s (configurable via `QDRANT_TIMEOUT_MS`). Sur timeout → `null` → fallback mots-clés immédiat.

- [~] Index `CREATE INDEX` des migrations 003-005 absents de `sql/schema.sql`
  - Diagnostic révisé : `sql/schema.sql` est un fichier hérité non utilisé. Le vrai template est `server/migrations/002-tenant-schema-template.sql` et contient déjà tous les index (lignes 326-351). Nouveaux tenants non impactés.
  - Fait : `sql/schema.sql` annoté comme hérité.

### Mineur

- [ ] `recalcTotals` TVA (`tva.service.ts:41-62, 214, 269, 302`) — index `declaration_id` présent, laissé tel quel.
- [ ] Styles inline dans grosses tables (`BilanSMT.tsx:190-200`) — à externaliser en classes CSS.
- [x] `staleTime` plan comptable 1h → 24h
  - Fait : `src/lib/queries.ts:94` → `staleTime` et `gcTime` à `24 * 60 * 60 * 1000`.

### Propre (inchangé)

React Query configuré, bulk inserts paramétrés, pagination existante sur écritures/balance principale, pas de lodash/moment massifs, transactions ACID.

---

## 3. Duplication de code

### Haute

- [x] 72 vérifications `if (!schema) return 400` répétées
  - Fait : middleware `server/middleware/requireTenantSchema.ts` ajouté à la `tenantChain` dans `server/index.ts:149`. Le guard est désormais global ; les `if (!schema)` dans les routes sont redondants (défense en profondeur) et peuvent être retirés progressivement.

- [x] Quatre implémentations de `formatMontant`
  - `src/etats/ImportBalance.parsers.ts:130`, `src/etats/bilan/bilanSyscohadaCompute.ts:7`, `src/etats/TFT_helpers.ts:52` remplacées.
  - Fait : `src/utils/formatters.ts` ajoute `fmtMontant` (entiers, '0' si zéro) et `fmtMontantParens` (négatifs entre parenthèses). Les 3 fichiers source délèguent au module central.
  - `src/paie/utils/calculPaie.ts:147` (`formaterMontant` en FCFA avec `Intl.NumberFormat`) — conservé (usage spécifique devise).

- [ ] Pattern `fetch + setLoading + try/catch silencieux` dans 50+ composants
  - `src/comptabilite/TiersPage.tsx`, `Journaux.tsx`, `GrandLivre.tsx`, tous les `src/etats/notes/Note*.tsx`, etc.
  - Non traité (gros refactor). À faire : créer `useFetchEntity<T>` + normalisation (`Array | { data } | { tiers }`).

### Moyenne

- [x] `interface PlanCompte` / `CompteComptable` dupliquée
  - Fait : `server/types/comptes.ts` créé, consommé par `ecritures.ts`, `tva.ts`, `planComptable.ts` (alias `CompteComptable`).

- [ ] Endpoints API en dur (~255 occurrences) — à centraliser dans `src/constants/apiEndpoints.ts`.

- [x] `formatDate` dupliquée (diagnostic révisé : 2 variantes distinctes)
  - Fait : `src/utils/formatters.ts` ajoute `fmtRelativeTime` ("A l'instant / Il y a X min/h/j") et `fmtDayRelative` ("Aujourd'hui / Hier / dd/MM"). `NotificationBell.tsx` et `AssistantChat.tsx` migrés.

### Basse

- [x] Taux IS (28/25/33/1 %) et IBA (30/1,5 %) en dur dans `src/etats/ResultatFiscal.tsx`
  - Fait : `src/constants/taxation.ts` centralise `TAUX_IS_NORMAL/ECOLES/ETRANGER`, `TAUX_MIN_IS`, `TAUX_IBA`, `TAUX_MIN_IBA` + `OPTIONS_TAUX_IS`. Le `<select>` itère sur `OPTIONS_TAUX_IS.map` au lieu des `<option>` hardcodés.

### Propre (inchangé)

Schémas Zod centralisés, configuration API isolée dans `src/lib/api.ts`.

---

## 4. Patterns cgi-242 portés

- [x] `validate({ body, query, params })` combiné ajouté à `server/middleware/validate.ts` (en plus des `validateBody`/`validateQuery`/`validateParams` existants).
- [x] Regex schema tenant durci à `^tenant_[a-z0-9_]{1,55}$` (rejette `public`, `information_schema`, `pg_catalog`, `pg_toast`) — `server/utils/tenant.utils.ts`.
- [x] `assertSafeSchemaName` ajouté pour défense en profondeur avant raw queries.
- [x] `sanitizeSchemaName` durci : premier char lettre + longueur max 63. Tests 26/26 verts.
- [x] Middleware `requireTenantSchema` : valide présence + format du schéma avant de rentrer dans les routes.

---

## Reste à faire (ordre de priorité décroissant)

1. Hook `useFetchEntity<T>` + migration progressive des composants (haute valeur, gros effort).
2. Import TVA : calculs SQL au lieu de boucle JS (`tva.service.ts:354`).
3. `readFileSync` → `fs/promises` + cache unique au startup.
4. `src/constants/apiEndpoints.ts` et adoption progressive.
5. Retrait progressif des `if (!schema)` dans les routes (redondants avec `requireTenantSchema`).
6. Validation au startup des clés API (Anthropic).
7. Styles inline `BilanSMT.tsx:190-200` → classes CSS.
8. Import TVA `recalcTotals` optimisation (mineur, index déjà présent).

---

## Validation

- TypeScript strict : frontend + backend OK.
- Jest : 52/52 tests verts (inclut 26 tests `tenant.utils` dont 3 nouveaux critères `sanitizeSchemaName`).
- Commits :
  - `20e0e5e refactor: audit securite + perf + duplication (sprint 1 & 2)` — 18 fichiers, +421/-82.
  - `8d6e84f docs: mettre a jour le rapport d'audit avec l'etat post-sprint 1&2`.
  - Vague 3 (non committée) : taxation.ts, fmtRelativeTime/fmtDayRelative, types/comptes.ts, CORS anti-`*`, Qdrant timeout.
